- self.content_for(:html, :item) do
  /
    #{__FILE__}
  %div.activity
    = self.activity.name
    - if self.activity.copy? || self.activity.shared?
      %div.shared
        Shared by #{self.activity.copy? ? self.activity.copy_of.user.url : 'you'}
