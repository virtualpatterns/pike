- self.content_for(:html, :item) do
  /
    #{__FILE__}
  %div.message
    = self.message_state.message.subject
    %div.created
      Created #{RubyApp::Language.locale.strftime(self.message_state.message.created_at, Pike::Application.configuration.format.date.long)}
