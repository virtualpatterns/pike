- self.content_for(:html, :item) do
  /
    #{__FILE__}
  %div.project
    = self.project.name
    - if self.project.copy? || self.project.shared?
      %div.shared
        Shared by #{self.project.copy? ? self.project.copy_of.user.url : 'you'}
