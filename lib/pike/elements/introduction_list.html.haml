- self.content_for(:html, :item) do |_for, item, index|
  /
    #{__FILE__}
  %div.introduction{:user => item.user_source.url}
    %div.from
      = item.user_source.url
    %div.message
      = item.message
